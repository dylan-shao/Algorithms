class Deque:
  def __init__:
  self.stack1 = []
  self.stack2 = []

  def l_append(self, n):
    self.stack1.append(n)
  def r_append(self, n):
    self.stack2.append(n)
    def l_remove():
  if len(self.stack1):
    self.stack1.pop()
  def r_remove():
    if len(self.stack2):
      self.stack2.pop()
  def l_peek():

  def r_peek():

  def is_empty():
    return len(self.stack1) == 0 and len(self.stack2) == 0